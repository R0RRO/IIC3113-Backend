services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      DATABASE_URL: "file:/app/data/mingup.db"
      JWT_SECRET: "${JWT_SECRET}"
      PORT: "3001"
      CORS_ORIGIN: "${CORS_ORIGIN}"
    volumes:
      - ./data:/app/data
